import React from 'react';
import {Form} from '../components';

const Home = () => {
    return (
        <main className='main'>
            <Form/>
            <h1>home</h1>
        </main>
    );
};

export default Home;