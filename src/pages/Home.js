import React from 'react';
import {Form, Spinner} from '../components';

const Home = () => {
    return (
        <main className='main'>
            <Form/>
            {/*<Spinner/>*/}
        </main>
    );
};

export default Home;