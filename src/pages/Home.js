import React from 'react';
import {Form, CountryList} from '../components';

const Home = () => {

    return (
        <main className='main'>
            <Form/>
            <CountryList/>
        </main>
    );
};

export default Home;